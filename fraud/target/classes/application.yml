server:
  port: 8081
spring:
  application:
    name: fraud
  datasource:
    username: 'admin-fraud'
    password: 'password-frau'
    url: jdbc:postgresql://localhost:543fez3/fraud
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true